### SETUP 

```
git fetch
git pull origin main
git submodule update --init --recursive
```

You will need the last one for the subip, this isnt really a common thing in software but very common in hw so sorry if this is annoying